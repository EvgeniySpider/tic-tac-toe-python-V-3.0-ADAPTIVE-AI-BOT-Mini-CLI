# 🎮 TicTacToe Library

Библиотека для игры "Крестики-Нолики" с расширенным функционалом: настраиваемое поле, умный бот, система логирования и продвинутая аналитика.

## ✨ Возможности

- 🎯 **Настраиваемое поле** — от 2×2 до 9×9
- 🤖 **Умный бот** — адаптивный ИИ с приоритетами (атака → защита → центр)
- 👥 **Режим PVP** — игра вдвоём
- 🎨 **Цветная визуализация** — красочное поле с подсветкой победной линии
- 📊 **Автологирование** — сохранение всех партий
- 📈 **Расширенная аналитика** — винрейт, статистика, поиск по датам

## 🚀 Установка

### Требования
- Python 3.7+
- Poetry

### Установка проекта

```bash
git clone https://github.com/твой-username/tictactoe.git
cd tictactoe
poetry install
```

## 🎯 Быстрый старт

### Запуск через командную строку

```bash
# Вариант 1: через poetry run
poetry run python -m tictactoe_library

# Вариант 2: активировать venv вручную
poetry env info --path  # узнать путь к venv
& path/to/venv/Scripts/Activate.ps1  # Windows
python -m tictactoe_library
```

### Запуск из Python-кода

```python
import tictactoe_library

# Быстрая игра с выбором режима (PVP/Bot)
tictactoe_library.play()

# Игра против бота (без выбора)
tictactoe_library.play_bot()

# Игра PVP (без выбора)
tictactoe_library.play_pvp()

# Кастомное поле
from tictactoe_library import TicTacToe
game = TicTacToe(board_size=5, mode=True)  # 5×5, режим бота
game.play()
```

## 📊 Аналитика и статистика

Все игры автоматически логируются в `tic_tac_toe_history.txt`. Используй модуль `log` для просмотра статистики:

### Основные команды

```python
import tictactoe_library as ttl

# Полная история игр
ttl.log.show_history()

# Только ничьи
ttl.log.show_draws()

# Общая статистика (победы X/O, ничьи по режимам)
ttl.log.show_stats()
```

### Детальная аналитика

```python
# Винрейт в режиме бота
ttl.log.show_winrate('bot')

# Винрейт в режиме PVP
ttl.log.show_winrate('pvp')

# Победы конкретного игрока
ttl.log.show_win('X')
ttl.log.show_win('O')

# Последние N игр
ttl.log.show_last_games(10)

# Суммарное количество ходов за все игры
ttl.log.show_sum_moves()

# Суммарная площадь всех полей
ttl.log.show_sum_boards()

# Самая быстрая игра
ttl.log.show_fastest()

# Игры по конкретной дате
ttl.log.show_by_date('26.02.2026')

# Удалить историю (требуется подтверждение)
ttl.log.delete('delete')
```

## 🤖 Логика бота

Бот использует систему приоритетов:
1. **Атака** — завершить свою победную линию
2. **Защита** — блокировать победу игрока
3. **Центр** — занять центральную клетку
4. **Случайный ход** — выбрать свободную клетку

## 📝 Формат логов

```
[26.02.2026 18:30] Режим: PVP | Поле: 3x3 | Количество ходов: 7 | Итог: Победил X
[26.02.2026 18:35] Режим: Бот | Поле: 5x5 | Количество ходов: 15 | Итог: Ничья
```

## 🔧 Структура проекта

```
TicTacToe/
├── tictactoe_library/
│   ├── __init__.py         # Публичный API
│   ├── __main__.py         # Точка входа для -m флага
│   ├── game.py             # Основная логика игры
│   ├── analytics.py        # Менеджер истории и аналитики
│   └── exceptions.py       # Кастомные исключения
├── pyproject.toml
├── poetry.lock
└── README.md
```

## 🛡️ Обработка ошибок

Библиотека использует кастомные исключения:

```python
from tictactoe_library import (
    BoardSizeTypeError,      # Неверный тип размера поля
    BoardSizeValueError,     # Размер вне диапазона 2-9
    CellOccupiedError,       # Клетка занята
    InvalidPositionError,    # Позиция вне поля
    InvalidInputError        # Некорректный ввод
)
```

## 🎨 Визуальные особенности

- ❌ **X** — красный цвет
- ⭕ **O** — зелёный цвет
- ✨ **Победная линия** — яркая подсветка

## 🛠️ Полезные команды Poetry

```bash
# Информация о venv
poetry env info

# Путь к интерпретатору
poetry env info --path

# Список пакетов
poetry show

# Добавить зависимость
poetry add 

# Обновить зависимости
poetry update
```

## 📄 Лицензия

Проект создан в образовательных целях.

## 🤝 Вклад в проект

Приветствуются issues и pull requests!

---

**Приятной игры!** 🎉